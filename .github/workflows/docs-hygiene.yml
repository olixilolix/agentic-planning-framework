name: Framework Hygiene

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate framework docs, links, and generated files
        shell: pwsh
        run: ./scripts/validate-docs.ps1
